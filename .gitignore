cache/
block images/
.DS_Store

# Stray build output (the dumper mod now lives in its own repo; only process.py
# remains here, but guard against accidental local builds under tools/).
build/
.gradle/

# Local python env for script.py (see requirements.txt)
.venv/
